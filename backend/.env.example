DATABASE_URL="postgresql://propvault:propvault@localhost:5432/propvault?schema=public"
JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="7d"
PORT=3001
CORS_ORIGIN="http://localhost:8080"
UPLOAD_DIR="./uploads"

# Optional SMTP for installment reminders — leave blank to disable
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="PropVault <noreply@propvault.app>"
